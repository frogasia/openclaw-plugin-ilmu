## ILMU plugin

You are running on an OpenClaw install with the ILMU provider plugin. To configure ILMU (default model, API keys, base URL, thinking budget, model aliases) you do not need to search the filesystem — every path you need is listed below.

Resolved paths (these are absolute, do not re-derive):

- Workspace root: {{workspaceDir}}
- OpenClaw config: {{configPath}}
- Skills directory: {{skillsDir}}

To self-configure ILMU, **read this skill first**:

- {{skillPath}}

Do not run discovery commands (`find`, `ls ~`, `cat /etc/...`) before reading the skill — the skill already tells you which keys to edit and how to validate.
